/**
 * @core/render — U4 export surface (unit-of-work.md import 契约):
 * RenderBackend port (RenderFrame / RenderJob / VideoResult) + the P1
 * FfmpegComposeBackend implementation.
 */
export * from "./types";
export * from "./ffmpeg-backend";
